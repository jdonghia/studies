The hidden scope property allows the function to access persisted data from its enclosing scope, if can't find the reference on its local scope

- Closures
- Closures
  - Backpack
  - `[[scope]]` defined at compile time
  - [ ] maintain data from functions enclosed scope / (lexical environment)
        The hidden scope property allows the function to access persisted data from its enclosing scope, if can't find the reference on its local scope

- ESM Modules auto strict



