- difference between double equals triple equals (they both make type comparisson) if the type matches, they work exactly the same. The double equals will coerce with type dont matches into numbers.

- boolean corner case for double equals. avoid because if the type dont match, it will coerce false and true to a number

- negative zero for stationary direction. 

Types are the same use double equals

non-primitive references compare with triple equals to avoid coercion in some way

typeof comparisson that returns a string and do not throw an error

NaNs

How Typescript differs from Javascript

- double equals
- triple equals
- undeclared x undefined x unitialized




