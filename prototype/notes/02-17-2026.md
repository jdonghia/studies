- every function (except for arrow functions) has a prototype property, which points to an empty object
- the functions prototype has a constructor property which points to the function itself

- reusable methods can be attached to the function's prototype

```javascript
function Foo() {}

Foo.prototype.bar = function () {};
```


- OOP is different from Prototype Delegation Pattern
- Link != instance
- Link != copy

Class and prototypes

class is a "syntatic sugar" for 

attaching methods on function prototype


object.create for creating a empty object and returning it
__proto__ of the object is the functions prototype

- classes this are confusing because it will always depend on the callsite

- instances have copy of methods, that's why





