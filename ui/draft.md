- remove/dettach c++ elements also update the view

- conditionals on remove()


- UI component: recursively create/replace (replaceChildren) same nodes 
- dettached elements
- Conditional without need to reason
- focus is lost in the current 

UI components describes the relation between data an view. 

```javascript
```

Create elements with arrays and template string

- Virtual DOM with string interpolation

- JS is loaded by HTML that is read by the browser
