- querySelector also works in nested elements

- adding a script blocks the afterward parse (async, defer)

- defer is wait for the html parse to load
- async will run the script when finishes loading

DOMContentLoaded event = when html is parsed 
x
load: when everything is loaded


onclick (a single one received)
x 
addEventlistener(onclick) (receive multiples)

removeEventListener is useful basically on SPAs

dispatch events on DOM (create custom events)

- starting with capital to singletons scripts

- MPA multiple htmls
- SPA one html

- SPA fakes it the path. It does not actually comes from the server

- Web Components

Proxies to incerpet when object changes

